{
  "name": "claw-staffer-functions",
  "description": "Firebase Cloud Functions for Claw Staffer — billing & account management",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && firebase deploy --only functions",
    "serve": "npm run build && firebase emulators:start --only functions"
  },
  "engines": { "node": "20" },
  "main": "lib/index.js",
  "dependencies": {
    "firebase-admin": "^13.6.1",
    "firebase-functions": "^6.3.2",
    "stripe": "^20.3.1"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.3",
    "firebase-functions-test": "^3.4.0"
  },
  "private": true
}
