# ============================================
# Claw Staffer Desktop (Windows) — Environment
# ============================================
# Copy this file to .env and fill in the values you need.
# The app works without any .env file — all values have sensible defaults.
# Place this file in the same directory as the .exe or project root.

# --- Core ---
# JWT_SECRET=change-me-to-a-random-secret-string
# ADMIN_PASSWORD=admin123

# --- AI Provider API Keys ---
# API keys are managed per-user in the app UI (Settings → API Keys).
# They are NOT read from environment variables.

# --- Ollama (local LLM) ---
# Ollama runs on localhost by default. No key needed.
# OLLAMA_BASE_URL=http://localhost:11434

# --- Telegram Bot Token (optional — you can also add bots via the UI) ---
# TELEGRAM_BOT_TOKEN=123456:ABC-DEF...

# --- Firebase Auth ---
# The app auto-detects firebase-service-account.json in the app directory.
# You can also set the path explicitly:
# FIREBASE_SERVICE_ACCOUNT_PATH=C:\path\to\firebase-service-account.json
# FIREBASE_PROJECT_ID=auto-staff-ai

# --- Integration OAuth Credentials (optional) ---
# GITHUB_CLIENT_ID=
# GITHUB_CLIENT_SECRET=
# GOOGLE_DRIVE_CLIENT_ID=
# GOOGLE_DRIVE_CLIENT_SECRET=
# VERCEL_CLIENT_ID=
# VERCEL_CLIENT_SECRET=
# NETLIFY_CLIENT_ID=
# NETLIFY_CLIENT_SECRET=

# --- Docker (optional) ---
# DOCKER_HOST=npipe:////./pipe/docker_engine

# --- Advanced ---
# LOG_LEVEL=info
# NODE_ENV=production
